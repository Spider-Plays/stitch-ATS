/**
 * Material Design 3 reference for Stitch ATS.
 * @see https://m3.material.io/
 *
 * Blue/teal seed — tokens in src/index.css (--primary, --surface-container, etc.)
 * and map to Tailwind via tailwind.config.js.
 *
 * Component mapping:
 * - Navigation drawer → Sidebar (m3-navigation-drawer)
 * - Top app bar → Header (m3-top-app-bar)
 * - Filled / tonal / outlined / text buttons → Button + .btn-* utilities
 * - Elevated card → .app-card (elevation level 1)
 * - Outlined text field → .app-input
 * - Filter chips → .filter-chip / .filter-chip-active
 */

export const M3_DOC_URL = 'https://m3.material.io/'
